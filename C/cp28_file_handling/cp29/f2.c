#include<stdio.h>
int main()
{
    FILE *f = fopen("f1.txt","r");
    int n=3;
    char ch;
    int c;
    // while(1)
    // {
       ch = fgetc(f);
    //    printf("%c",ch);
    //    if(ch == EOF)break;
    // }

    return 0;
}
